package jetbrains.mps.core.syntax.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class SPrioKind_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<SPrioKind> constants = ListSequence.fromList(SPrioKind.getConstants()).iterator();
    while (constants.hasNext()) {
      SPrioKind constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<SPrioKind> constants = ListSequence.fromList(SPrioKind.getConstants()).iterator();
    while (constants.hasNext()) {
      SPrioKind constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    SPrioKind constant = SPrioKind.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
