package jetbrains.mps.core.notation.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class SNotationStyleColorKinds_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<SNotationStyleColorKinds> constants = ListSequence.fromList(SNotationStyleColorKinds.getConstants()).iterator();
    while (constants.hasNext()) {
      SNotationStyleColorKinds constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<SNotationStyleColorKinds> constants = ListSequence.fromList(SNotationStyleColorKinds.getConstants()).iterator();
    while (constants.hasNext()) {
      SNotationStyleColorKinds constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    SNotationStyleColorKinds constant = SNotationStyleColorKinds.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
